# Backend API base URL (FastAPI MeetingMind server)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Set to "false" to use live FastAPI backend, or "true" to use demo mock data
NEXT_PUBLIC_USE_MOCKS=false
